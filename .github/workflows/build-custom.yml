name: Build Custom RustDesk

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build-windows:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "custom"
